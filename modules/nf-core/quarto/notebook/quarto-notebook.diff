Changes in component 'nf-core/quarto/notebook'
Changes in 'quarto/notebook/main.nf':
--- modules/nf-core/quarto/notebook/main.nf
+++ modules/nf-core/quarto/notebook/main.nf
@@ -28,6 +28,7 @@
     tuple val(meta), path("params.yml")                                                        , emit: params_yaml
     tuple val(meta), path("${notebook_parameters.artifact_dir}/*")                             , emit: artifacts  , optional: true
     tuple val(meta), path("_extensions")                                                       , emit: extensions , optional: true
+    tuple val("${task.process}"), val("${workflow.containerEngine ?: (task.executor == 'awsbatch' ? 'awsbatch' : (task.conda ? 'conda' : 'none'))}"), val("${((workflow.containerEngine || task.executor == 'awsbatch') ? task.container : task.conda) ?: 'Not configured'}"), emit: runtime_environment
     path "versions.yml"                                                                        , emit: versions          , topic: versions
     tuple val("${task.process}"), val('quarto'), eval('quarto -v')                             , emit: versions_quarto   , topic: versions
     tuple val("${task.process}"), val('papermill'), eval('papermill --version | cut -f1 -d" "'), emit: versions_papermill, topic: versions
@@ -85,12 +86,6 @@
         --execute-params params.yml \\
         --output ${prefix}.html
 
-    # Check that notebook package versions is exported
-    if [ ! -f versions.csv ]; then
-        echo "ERROR: versions.csv not found; the notebook must write out [tool,version] pairs used within it." >&2
-        exit 1
-    fi
-
     # Write notebook package versions to YAML
     cat <<- END_VERSIONS > versions.yml
     "${task.process}":

'modules/nf-core/quarto/notebook/environment.yml' is unchanged
Changes in 'quarto/notebook/meta.yml':
--- modules/nf-core/quarto/notebook/meta.yml
+++ modules/nf-core/quarto/notebook/meta.yml
@@ -107,6 +107,16 @@
           description: Quarto extensions used during report rendering.
           pattern: "*"
           ontologies: []
+  runtime_environment:
+    - - ${task.process}:
+          type: string
+          description: The name of the process
+      - "${workflow.containerEngine ?: (task.executor == 'awsbatch' ? 'awsbatch' : (task.conda ? 'conda' : 'none'))}":
+          type: string
+          description: The active runtime backend, such as a container engine, AWS Batch, `conda`, or `none`
+      - "${((workflow.containerEngine || task.executor == 'awsbatch') ? task.container : task.conda) ?: 'Not configured'}":
+          type: string
+          description: The resolved container image or Conda reference, or `Not configured` when no managed runtime is configured
   versions:
     - versions.yml:
         type: file

'modules/nf-core/quarto/notebook/tests/nextflow.config' is unchanged
'modules/nf-core/quarto/notebook/tests/main.nf.test.snap' is unchanged
'modules/nf-core/quarto/notebook/tests/main.nf.test' is unchanged
************************************************************
